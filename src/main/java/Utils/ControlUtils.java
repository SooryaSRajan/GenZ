package Utils;
import javassist.*;

public class ControlUtils {

    static final String IF_BLOCK = "if (%s) { %s }";
    static final String ELSE_IF_BLOCK = "else if (%s) { %s }";
    static final String ELSE_BLOCK = "else { %s }";

    static final String FOR_LOOP = "for (int %s = %s; %s < %s; %s++) { %s }";
    static final String FOR_LOOP_REVERSE = "for (int %s = %s; %s > %s; %s--) { %s }";

    //parse expression




}
